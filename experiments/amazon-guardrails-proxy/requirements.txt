# Core server
fastapi==0.115.0
uvicorn[standard]==0.30.0
websockets==12.0

# AWS SDK (Bedrock Guardrails)
boto3>=1.34.0

# OpenAI Realtime API client
openai==1.50.0

# Environment config
python-dotenv==1.0.1

# NOTE: No torch, no transformers, no BERT.
# Bedrock Guardrails handles PII detection and injection detection.
# This reduces install size from ~2.5GB to ~50MB.

certifi
